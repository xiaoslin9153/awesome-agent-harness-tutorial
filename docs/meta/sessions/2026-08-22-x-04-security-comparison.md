---
date: 2026-08-22
topic: X-04 安全与审批对比初稿
status: 推送前检查进行中
---

# X-04 安全与审批对比初稿

## 目标

基于三家框架章节和 Sandbox、Approval、Prompt Injection 机制，完成 X-04 的中文 Draft 和 Polish；保留批量 Implementation Review 清单，通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/04-comparisons/security.md`、目录状态、本会话记录和总进度表。
- 范围外：持久化恢复、事件模型完整拆解和实验验证。

## 记录与证据

- 已确认 X-03 已由 `f341efe` 推送，`main` 与 `origin/main` 同步且工作区干净。
- 已核对 Reasonix 的策略链与 OS 沙箱、DeepSeek Harness 的审批失败关闭与 Landlock、Pi 的项目信任与外置容器化边界。
- 三家共同点是提示词防御不等于访问控制；差异在于默认自主度、审批服务缺失时的行为和隔离层位置。

## 成功标准

- 章节包含安全决策图和对照表，覆盖信任输入、权限策略、审批终态、沙箱边界和注入响应。
- 明确比较失败关闭、显式逃逸/降级、凭证位置和审计投影。
- Polish 通过后语言清晰；平台实现细节保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 X-04 是 X-03 后的下一个横向对比章节。
- 2026-08-22：主 Agent 完成 X-04 Draft，覆盖安全决策链、权限策略、审批语义、隔离技术和注入兜底。
- 2026-08-22：主 Agent 直接完成 Polish，统一信任输入、强制访问控制、失败关闭、显式降级和安全投影术语。

## 自检证据

- 已核对 diff 只包含 X-04 章节、TOC、本会话记录和总进度表。

## 下一步

完成两套链接检查后提交推送；X-05 按 TOC 继续。
