---
date: 2026-08-22
topic: 全面复审与教材发布
status: 进行中
---

# 全面复审与教材发布

## 会话目标

1. 按最新写作标准复审产品记录、进度表、对比账本、证据索引、会话记录、目录、术语表、站点配置和既有公开章节。
2. 按目录顺序完成核心概念、机制教材、三家框架深拆、横向对比、实验、案例、面试题库、评测和站点整合。
3. 使用批量草稿模式推进，保留 `pending_review` 清单与源码锚点；初稿全部完成后统一执行 Implementation Review。
4. 只有事实审查通过后才允许公开章节进入 `published`。
5. 最终完成全站链接检查、构建检查、页面内容检查、GitHub Pages 部署验证、进度同步和干净推送。

## 范围

- 范围内：全部治理记录、公开教材、证据索引、站点构建、部署验证和发布状态同步。
- 范围外：更新三家外部仓库快照；如必须更新，先重新锚定版本并另开会话。

## 初始假设

- 三家框架继续使用对比账本固定的 `aa82b2f`、`b150a55`、`c49906e` 快照。
- 现有站点构建器和链接检查是发布基线，先复用，不因教材写作引入生成器迁移。

## 成功标准

- 所有公开章节存在且通过 Polish 与 Implementation Review。
- 所有 `pending_review` 清零，可发布章节状态为 `published`。
- 对比账本和证据索引覆盖框架深拆所需维度。
- 本地构建、链接检查、页面内容检查和线上部署验证全部通过。
- 工作区干净，`main` 与远端一致。

## 执行记录

### 2026-08-22

1. 已读取 `AGENTS.md`、`docs/meta/session-hook.md`、`docs/product/product-design.md`、`docs/comparisons/framework-comparison-ledger.md` 和总进度表。
2. 已确认工作区初始状态干净且 `main` 与远端一致。
3. 已发现进度表存在滞后：P0/P1/P2/P3/P6 的部分状态与已交付文件或站点流水线不一致；本会话将随复审逐项修正。
4. 已运行 `npm --prefix site run check:links`，7 个 Markdown 文件链接全部通过；已运行带 `/awesome-agent-harness-tutorial/` 基础路径的站点构建，生成 6 个页面。
5. 已复审进度表、目录和公开章节 Front Matter，修正 P0/P1/P6 阶段状态、G11/G12/F00/S02-S05 任务状态、C-02 目录链接缺失、术语表审查元数据和“已完成”与“可发布”混淆问题。

### 决策

目录中的 ✅ 只表示章节初稿与当前验收完成；只有 Implementation Review 通过后才允许把 Front Matter 改为 `published`。术语表 Polish 通过，但保留 Implementation Review 为待审。

### 2026-08-22 执行调整

Goal Agent 因并发 Subagent 过多触发服务商 HTTP 429。后续改为严格串行：主 Agent 先完成单节 Draft，再依次执行 Polish、自检、提交、Deploy 检查和进度同步。任一时刻只保留一个 Subagent；一节完成后才进入下一节。恢复时按 60 秒、120 秒、300 秒退避，上限 900 秒，并从原章节原阶段继续。

### 2026-08-22 策略修订

Polish Subagent 连续返回 HTTP 402 后，维护者决定进一步移除 Subagent 执行路径。现行策略是主 Agent 单执行者串行完成 Draft、Polish、Implementation Review、部署检查和进度同步；不再创建 Subagent，也不再维护 402/429 退避流程。历史记录保留故障背景，但不再是现行执行规范。

## 开放问题

1. 待运行链接检查和站点构建，确认当前基线是否通过。
2. 待盘点既有证据索引是否足以支撑批量草稿。

## 下一步

1. 运行链接检查与站点构建，记录基线结果。
2. 复审站点配置、目录、术语表、证据索引和既有章节。
3. 建立批量草稿清单和事实锚点，再按章节顺序撰写。
