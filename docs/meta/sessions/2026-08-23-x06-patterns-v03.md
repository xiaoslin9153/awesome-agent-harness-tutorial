# 会话：X-06 设计模式与反模式 v0.3 重写

## 状态与目标

- 状态：已完成。
- 日期：2026-08-23。
- 目标：把 `tutorial/zh-CN/04-comparisons/patterns.md` 升级到 v0.3 标准，完成 Polish 和 Implementation 自检，同步状态记录，并通过本地构建、提交推送和线上页面验证。
- 范围内：只修订 X-06 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不修改 `external/`，不引入新源码锚点，不推进第五章实验，不开始批量终审。

## 已完成内容

1. 审计旧稿：旧稿已有六条模式、十个反模式和迁移检查单，但缺少 learning contract、完整因果链、第二个 Mermaid 图、带因果解释的故障链、递进交接和显式证据分层。
2. 复核继承证据：确认 X-01 到 X-05 的 learning contract 和自检状态；X-06 不新增源码行号，避免复制后出现两份漂移的事实表。
3. 重写章节：新增约束优先决策图和模式不变量图、核心不变量、六条「直觉→机制→边界」模式、十个反模式的诱惑解释、七条故障链、注入加预算的复合因果链、设计取舍和迁移检查单。
4. Front Matter 记录主 Agent Polish 与 Implementation Review 通过。
5. 同步 TOC 中 X-06 状态、B-004 执行顺序和总进度表 X06 行，并声明第四章横向对比初稿完成。

## 决策与证据

1. 模式章采用证据分层：X-06 引用比较章，比较章引用机制章，机制章绑定固定快照和 path:line。
2. 保留旧稿中已验证价值的六条模式和十个反模式，但每条补充触发条件、破坏的不变量和失效信号。
3. 固定快照仍为 Reasonix `aa82b2f`、DeepSeek Harness `b150a55`、Pi `c49906e`；本章只引用，不重复声明新事实。
4. 把「预算阶梯」从泛泛的成本建议改成降级可见性不变量，防止静默裁剪关键任务约束。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 内容结构 | 2 张 Mermaid 图；7 个带因果解释的反例；1 条覆盖触发、拦截、预算、观察和对账的完整因果链。 |
| 继承证据 | 六条模式的三家例子分别链接到 X-03、X-04 或 X-05；总体证据入口链接到 X-01 至 X-05。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 45 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 44 个页面。 |
| Git 检查 | `git diff --check` 通过；变更限定在 X-06 及必要状态同步。 |
| 提交部署 | 重写提交 `bf193c1` 推送成功；Deploy Pages run `32628328980` 成功。 |
| 线上页面 | `/zh-CN/04-comparisons/patterns/` 返回 HTTP 200，并包含标题、故障模式和完整因果链。 |

## 开放问题

1. 批量终审需复核 X-01 到 X-05 中被本章引用的结论在最终版本中没有语义漂移。
2. 「预算阶梯」中 Pi `prepareNextTurn` 行为例子来自 X-03；后续如机制章细化 API 行号，应回到 X-03 同步。

## 下一步

1. X-06 重写、本地验证、提交推送和线上检查已完成；第四章横向对比初稿升级完成。
2. 下一次迭代进入第五章 L-01《最小 Agent Run 实验》。
3. 批量终审时复核 X-01 到 X-05 与 X-06 的引用一致性。
