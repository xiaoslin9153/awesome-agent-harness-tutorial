# 会话：E-01 Harness 评测框架 v0.3 新建

## 状态与目标

- 状态：进行中，待提交和部署验证。
- 日期：2026-08-23。
- 目标：新建 `tutorial/zh-CN/08-evaluation/evaluation-framework.md`，把已验证机制转成评测信号与门禁体系，完成 Polish 与 Implementation 自检，同步记录并完成部署检查。
- 范围内：只新增 E-01 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不实现评测代码，不修改既有章节，不引入新源码事实，不推进 G-01，不开始批量终审。

## 已完成内容

1. 输入审计：E-01 原为「未开始」；依赖的第四章对比和 M-12/M-14/M-15/M-16 均已通过 v0.3 自检，L/CS 系列提供确定性输出证据。
2. 证据核对：重新核对 16 个直接源码锚点区间的行数，全部存在且匹配，覆盖可观测事件、telemetry schema、成本账本、注入拦截、并发纪律和调度配额。
3. 新建框架：8 类评测信号（协议完整性、投影一致性、安全拦截、幂等性、恢复资格、成本归因、并发纪律、审计闭环）映射到不变量与判定方式。
4. 门禁体系：G0 快速 / G1 完整 / G2 压力三级门禁，定义触发时机、包含信号和失败动作；replay bundle 定义最小字段并要求无效报告不放行修复。
5. 内容结构：2 张 Mermaid 图（评测流水线和信号门禁映射）；6 条反模式；一条「静默丢 correction 被 signal 2 拦截」的完整因果链。
6. Front Matter 记录主 Agent Polish 与 Implementation Review 通过。
7. 同步 TOC 中 E-01 链接和状态、B-004 执行顺序和进度表 L01 行。

## 决策与证据

1. 不变量驱动信号：每个信号声明保护哪条不变量，失败可翻译成破坏的保护类型。
2. 结构化判分优先：安全、幂等、恢复信号基于事件而非文本相似度，抗 prompt 游戏化。
3. 成本全量归因：引用 DSH UsageRow 覆盖 failed/retried/synthetic/aborted 与 Reasonix 越界轴命名。
4. 直接引用的 16 个锚点当日复核行数；其余事实通过章节链接分层引用。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 源码锚点 | 16 个直接引用区间逐个核对，expected/actual 行数一致。 |
| 内容结构 | 2 张 Mermaid 图；6 条反模式；1 条完整因果链；8 个信号均有不变量和判定方式。 |
| 继承证据 | 引用 M-12/M-14/M-15/M-16、X-04/X-06、L-01/L-03/L-04 和 CS-01。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 50 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 49 个页面。 |
| Git 检查 | `git diff --check` 通过；当前变更限定在 E-01 及必要状态同步。 |
| 提交部署 | 待执行。 |

## 开放问题

1. replay bundle 的保留期与敏感数据脱敏方案需要专门设计；本章保留在自检问题中。
2. G2 压力矩阵的具体组合（并发度 × 分支深度）待维护者确认后细化。
3. 批量终审时若 M-12/M-15 锚点调整，本章对照表必须同步。

## 下一步

1. 只提交本章及状态同步文件，推送后检查 GitHub Actions 和线上页面。
2. 回填部署证据并标记会话完成。
3. 下一次迭代处理 G-01《术语表》终审升级。
