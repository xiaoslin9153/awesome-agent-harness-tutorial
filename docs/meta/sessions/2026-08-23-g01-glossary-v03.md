# 会话：G-01 术语表 v0.3 重写（最后章节）

## 状态与目标

- 状态：进行中，待提交和部署验证。
- 日期：2026-08-23。
- 目标：把 `tutorial/zh-CN/09-glossary/glossary.md` 升级为全书词汇基准，完成 Polish 与 Implementation 自检，同步状态记录并通过部署检查。
- 范围内：只修订 G-01 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不修改其他章节，不引入新源码事实，不启动批量 Implementation Review。

## 已完成内容

1. 审计旧稿：旧稿只有中英对照定义，缺少 learning contract、权威章节链接、失效边界、混用辨析、v0.3 结构和终审接口。
2. 重写结构：
   - 生命周期 / 状态 / 治理三组术语表，每条含中文、英文、定义、权威章节、失效边界。
   - 8 组易混术语辨析（Turn vs Run、Event vs Fact、Projection vs Authority、Denied vs Undecided、Replayed vs Executed、Trust vs Sandbox、Retry vs Replay、Checkpoint vs UI state）。
   - 通用术语与三家框架专名的映射表。
3. 内容结构：2 张 Mermaid 图（术语地图与混淆分类）；5 条故障模式；一条「审查 PR 时用术语表消除歧义」的完整因果链。
4. Front Matter 记录主 Agent Polish 与 Implementation Review 通过。
5. 同步 TOC 中 G-01 状态、B-004 执行顺序和进度表 L01 行，并声明 B-004 章节初稿升级完成。

## 决策与证据

1. 权威章节唯一：每个术语只链接一个深入定义的章节，本表不复制论证，避免多处维护漂移。
2. 失效边界强制：每行必须说明术语何时不再适用，防止教学模型被当成生产保证。
3. 通用词与专名分层：横向对比使用本表通用词，框架内部命名保留在框架章。
4. 修订协议：改义必须追加带日期的决策记录，禁止静默替换。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 内容结构 | 2 张 Mermaid 图；三组三栏术语表；8 组混用辨析；1 条完整因果链。 |
| 继承证据 | 所有权威章节均为已通过 v0.3 自检的 C/M/X/L/CS/Q/E 页面。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 50 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 49 个页面。 |
| Git 检查 | `git diff --check` 通过；变更限定在 G-01 及必要状态同步。 |
| 提交部署 | 待执行。 |

## 开放问题

1. 多语言站点上线后，中英对照如何与 en 版本保持同步。
2. 新术语准入标准（是否要求先有机制章）待维护者确认。

## 下一步

1. 只提交本章及状态同步文件，推送后检查 GitHub Actions 和线上页面。
2. 回填部署证据并标记会话完成。
3. 输出最终清单和批量 Implementation Review 计划，等待维护者指令。
