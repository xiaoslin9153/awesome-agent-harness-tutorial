---
date: 2026-08-22
topic: M-14 Sub-agent 与并发初稿
status: 推送前检查完成
---

# M-14 Sub-agent 与并发初稿

## 目标

按串行流水线完成 M-14 的中文 Draft 和 Polish，保留批量 Implementation Review 清单；通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/02-harness-mechanics/subagent-concurrency.md`、目录状态、本会话记录和总进度表。
- 范围外：成本延迟优化专题、多 Agent 组织架构、具体框架源码审查和批量事实审查。

## 记录与证据

- 已确认 M-13 已由 `27a0eee` 推送，`main` 与远端同步且工作区干净。
- 本章区分并行工具调用、Sub-agent 委派和多任务编排；重点处理契约、隔离、汇合、失败和取消。
- 框架对照沿用三个快照版本；具体并发能力保留待审。

## 成功标准

- 章节包含理想模型图、双读者解释、并发层级、委派契约、状态汇合和常见坑。
- 明确父 Run 仍负责权限、预算、审计和最终提交。
- Polish 通过后语言清晰；框架行为保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 M-14 是 M-13 后的下一个章节。
- 2026-08-22：主 Agent 完成 M-14 Draft，新增 Sub-agent 与并发章节。
- 2026-08-22：主 Agent 直接完成 Polish，统一并行工具、子智能体、任务契约、状态隔离、汇合策略和取消传播术语，强调父 Run 保留权限、预算、审计和最终提交。

## 自检证据

- `node scripts/check-links.mjs` 通过。
- `cd site && npm run check:links` 通过。
- 已检查 diff 只包含 M-14 章节、TOC、本会话记录和总进度表。

## 下一步

M-14 推送后按 TOC 顺序启动 M-15 成本与延迟。
