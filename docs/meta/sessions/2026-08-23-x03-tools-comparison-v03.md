# 会话：X-03 工具协议对比 v0.3 重写

## 状态与目标

- 状态：进行中。
- 日期：2026-08-23。
- 目标：把 `tutorial/zh-CN/04-comparisons/tools.md` 升级到 v0.3 深度标准，完成 Polish 和 Implementation 自检，同步索引记录，并通过本地构建和线上部署检查。
- 范围内：只修订 X-03 公开章节、TOC 状态、B-004 进度、本会话记录和必要的进度总览。
- 范围外：不修改 `external/`，不引入新源码快照，不批量重写其他章节，不做统一 Implementation 终审。

## 已完成内容

1. 审计旧稿后确认新稿覆盖统一生命周期、定义面、校验治理、并发取消、结果投影、七个故障模式、完整因果链和三家取舍。
2. 核对 X-03 不新增源码锚点的约束；框架结论复用 M-03 到 M-07 和三个框架工具专题中已验证的锚点。
3. 补充 M-03 到 M-05 的显式证据链链接，避免「可追溯到机制章节」停留在无路径的声明。
4. 更新 Front Matter，将 Polish 和 Implementation 自检标记为当日主 Agent 通过。
5. 将 `tutorial/zh-CN/TOC.md` 中 X-03 状态升级为已按 v0.3 重写。
6. 将 B-004 执行进度更新为 X-01 到 X-03 完成，下一步推进 X-04。

## 证据与验证

| 项目 | 证据 |
| --- | --- |
| 内容结构 | 新稿包含 2 张 Mermaid 图、7 个带因果解释的故障模式、1 条跨三家的完整因果链。 |
| 复用证据 | M-03 到 M-07 已在各自 v0.3 章节通过 Implementation 自检；本章只做机制级对照，不重复声明新源码事实。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 45 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 44 个页面。 |
| Git 检查 | `git diff --check` 通过；变更限定在 X-03、TOC、B-004 和本会话记录。 |
| 部署检查 | 推送后检查 GitHub Actions 和受影响页面；当前本节先记录为待执行。 |

## 决策

1. 保持比较章不引入新的固定快照锚点；所有框架行为通过既有机制章和框架专题承接。
2. 把证据链从隐含 ID 改成显式相对链接，使读者可以从对比表回到可核对的机制页。
3. 一次提交仍限定为 X-03 重写及其必要状态记录，避免和其他章节混入同一变更。

## 开放问题

1. X-03 的批量终审仍需统一复核所有引用机制页中的源码锚点和事实表述。
2. 「实现精妙之处」目前并入设计取舍叙述；若后续维护者认为必须独立成节，应在流水线模板层面统一调整。

## 下一步

1. 执行链接检查、构建和 `git diff --check`。
2. 只提交 X-03 相关文件，推送后验证 Actions 和页面。
3. 将部署结果回填本记录，再按仓库约定单独记录部署状态。
