# 会话：Q-03 安全与系统设计题库 v0.3 新建

## 状态与目标

- 状态：已完成。
- 日期：2026-08-23。
- 目标：新建 `tutorial/zh-CN/07-interview/security-design.md`，把第二、四章安全结论转成带威胁模型的系统设计题库，完成 Polish 与 Implementation 自检，同步记录并完成部署检查。
- 范围内：只新增 Q-03 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不修改既有机制章节，不引入新源码事实，不推进 E-01，不开始批量终审。

## 已完成内容

1. 输入审计：Q-03 原为「未开始」；依赖的 M-06/M-07/M-15/M-16 与 X-04 均已通过 v0.3 自检。
2. 证据核对：重新核对 10 个直接源码锚点区间的行数，全部存在且匹配，覆盖审批对象、monotonic deny、沙箱契约、进程环境裁剪、注入边界、预算账本和调度配额。
3. 新建题库：8 个系统设计场景 S1-S8，每题包含威胁模型、参考设计链（控制点/失效边界/默认行为/测试）、常见错误和证据入口。
4. 内容结构：2 张 Mermaid 图（威胁建模路线图和纵深防御循环图）；5 条反模式；一条「网页注入 → 双层拦截 → 审计重建」的完整因果链。
5. Front Matter 记录主 Agent Polish 与 Implementation Review 通过。
6. 同步 TOC 中 Q-03 链接和状态、B-004 执行顺序和进度表 L01 行。

## 决策与证据

1. 采用 threat-model-first 出题：先写 attacker/capability/impact，再谈控制点，避免名词堆砌。
2. 每个方案必须回答依赖不可用时的默认行为，并区分 fail closed 与显式 downgrade。
3. 直接引用的 10 个锚点当日复核行数；其余事实通过章节链接分层引用。
4. 把预算列为安全控制：引用 M-15 的 token→cost→wall 越界命名和 append-only usage ledger。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 源码锚点 | 10 个直接引用区间逐个核对，expected/actual 行数一致。 |
| 内容结构 | 2 张 Mermaid 图；5 条反模式；1 条完整因果链；8 个场景均有威胁模型和设计链。 |
| 继承证据 | 引用 M-06/M-07/M-14/M-15/M-16、X-04/X-06、L-04 和 CS-02。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 49 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 48 个页面。 |
| Git 检查 | `git diff --check` 通过；当前变更限定在 Q-03 及必要状态同步。 |
| 提交部署 | 新建提交 `ccc5de0` 推送成功；Deploy Pages run `32632272324` 成功。 |
| 线上页面 | `/zh-CN/07-interview/security-design/` 返回 HTTP 200，并包含标题、故障模式和完整因果链。 |

## 开放问题

1. 显式降级的 owner/过期机制需要专门设计文档；本章只在取舍表中提出。
2. 批量终审时若 M-15 的 DSH ledger 锚点调整（该锚点位于 Pi harness 文档），S5 必须同步说明来源层级。

## 下一步

1. Q-03 新建、锚点核对、本地验证、提交推送和线上检查已完成；第七章题库完成。
2. 下一次迭代处理 E-01《Harness 评测框架》。
3. 批量终审时同步复核 M-15 的 DSH ledger 锚点来源层级。
