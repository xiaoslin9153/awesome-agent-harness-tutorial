---
date: 2026-08-22
topic: M-03 Tool Schema 与调用协议初稿
status: 暂停
---

# M-03 Tool Schema 与调用协议初稿

## 目标

按串行流水线完成 M-03 的中文 Draft 和 Polish，保留批量 Implementation Review 清单；通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/02-harness-mechanics/tool-schema.md`、目录状态、本会话记录和总进度表。
- 范围外：具体执行隔离、审批策略、结果截断和批量事实审查。

## 记录与证据

- 已确认 M-02 已由 `3068f28` 推送，`main` 与远端同步且工作区干净。
- 本章只处理工具声明、参数校验、调用意图和协议契约；执行副作用与沙箱留给后续章节。
- 框架对照沿用三个快照版本，注册与校验路径保留待审。

## 成功标准

- 章节包含理想模型图、双读者解释、Schema 字段、调用生命周期、错误分类和常见坑。
- 区分模型可见描述、运行时实现和治理约束。
- Polish 通过后语言清晰；框架行为保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 M-03 是 M-02 后的下一个章节。
- 2026-08-22：主 Agent 完成 M-03 Draft；首次应用补丁因编号列表缺号未生效，已确认无半写内容后重写成功。
- 2026-08-22：主 Agent 直接完成 Polish，统一工具声明、调用意图、校验、治理和观察术语，强化唯一调用 ID 和错误分层。

## 自检证据

- `node scripts/check-links.mjs` 通过。
- `cd site && npm run check:links` 通过。
- 已检查 diff 只包含 M-03 章节、TOC、本会话记录和总进度表。

## 下一步

M-03 推送后按 TOC 顺序启动 M-04 Tool 执行与副作用。
