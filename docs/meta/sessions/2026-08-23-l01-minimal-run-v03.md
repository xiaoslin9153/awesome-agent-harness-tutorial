# 会话：L-01 最小 Agent Run 实验 v0.3 重写

## 状态与目标

- 状态：已完成。
- 日期：2026-08-23。
- 目标：把 `tutorial/zh-CN/05-labs/minimal-run.md` 升级到 v0.3 标准，核对并执行真实实验命令，完成 Polish 与 Implementation 自检，同步状态记录并通过部署检查。
- 范围内：只修订 L-01 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不修改 `labs/minimal-run` 代码，不修改 `external/`，不推进 L-02，不开始批量终审。

## 已完成内容

1. 审计旧稿：旧稿只有事件表、序列图和简短观察点，缺少 learning contract、理想模型图、机制深拆、故障链、完整因果链、显式源码行号和真实命令输出。
2. 核对实验实现：读取 `package.json`、README、`src/model.mjs`、`src/tools.mjs`、`src/run.mjs` 和测试文件，确认无依赖 ESM、structuredClone、10 turn 上限、失败观察和三条测试路径。
3. 执行验证：`npm start` 输出 10 条 JSONL 事件并以 `run_end(completed)` 结束；事件计数为 run_start 1、turn_start 2、assistant_message 2、tool_call 1、tool_result 1、turn_end 2、run_end 1；`npm test` 输出三条路径通过。
4. 重写章节：新增状态流程图和交互时序图、六条故障模式、完整因果链、设计取舍表、本地代码锚点和 C-02 框架对照。
5. Front Matter 记录主 Agent Polish 与 Implementation Review 通过。
6. 同步 TOC 中 L-01 状态、B-004 执行顺序和进度表 L01 行。

## 决策与证据

1. 不修改 lab 代码：当前实现已满足教学门禁；本章只修正教材与实现的描述差。
2. 明确两类失败边界：工具异常转为模型可见 isError 观察；fake model 脚本耗尽保持抛错，避免 fixture 错误伪装成领域结果。
3. 框架对照只引用 C-02 已验证结论，不为本章新造三家源码锚点。
4. 本地证据为 Node.js v26.7.0 下的一次实际执行；确定性假件使输出可重复。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 实验运行 | `cd labs/minimal-run && npm start` 成功。 |
| 实验测试 | `cd labs/minimal-run && npm test` 成功，3 条路径通过。 |
| 内容结构 | 2 张 Mermaid 图；6 个带因果解释的故障模式；1 条完整因果链。 |
| 源码锚点 | 引用 `labs/minimal-run/src/*.mjs` 和 test 的存在行号区间。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 45 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 44 个页面。 |
| Git 检查 | `git diff --check` 通过；变更限定在 L-01 及必要状态同步。 |
| 提交部署 | 重写提交 `984a188` 推送成功；Deploy Pages run `32628743764` 成功。 |
| 线上页面 | `/zh-CN/05-labs/minimal-run/` 返回 HTTP 200，并包含标题、故障模式和完整因果链。 |

## 开放问题

1. 批量终审需在不同 Node LTS 版本复跑实验，确认 `import.meta.filename` 与 structuredClone 行为说明仍准确。
2. 若后续把 turn 上限或 tools 注入改成配置参数，必须同步更新本章的「参数与环境」和取舍表。

## 下一步

1. L-01 重写、实验执行、本地验证、提交推送和线上检查已完成。
2. 下一次迭代处理 L-02《Context 膨胀实验》。
3. 批量终审时在不同 Node LTS 复跑最小实验。
