---
date: 2026-08-22
topic: M-02 Context 压缩与截断初稿
status: 暂停
---

# M-02 Context 压缩与截断初稿

## 目标

按串行流水线完成 M-02 的中文 Draft 和 Polish，保留批量 Implementation Review 清单；通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/02-harness-mechanics/context-compression.md`、目录状态、本会话记录和总进度表。
- 范围外：工具协议细节、持久化恢复、B 系列改造和批量事实审查。

## 记录与证据

- 已确认 M-01 已由 `620cf59` 推送，`main` 与远端同步且工作区干净。
- 本章承接 Context 组装的预算和优先级问题，只处理压缩、截断、摘要和可追溯性，不重复组装顺序。
- 框架对照沿用三个快照版本；具体压缩触发点和保留策略保留待审。

## 成功标准

- 章节包含理想模型图、双读者解释、压缩策略、截断边界、失败处理和常见坑。
- 区分无损裁剪、有损摘要和硬截断，并说明提交点与审计要求。
- Polish 通过后语言清晰；框架行为保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 M-02 是 M-01 后的下一个章节。
- 2026-08-22：主 Agent 完成 M-02 Draft，新增 Context 压缩与截断章节。
- 2026-08-22：主 Agent 直接完成 Polish，统一无损裁剪、结构化摘要、选择性保留和硬截断术语，强调来源不变、提交点不变和可追溯三条边界。

## 自检证据

- `node scripts/check-links.mjs` 通过。
- `cd site && npm run check:links` 通过。
- 已检查 diff 只包含 M-02 章节、TOC、本会话记录和总进度表。

## 下一步

M-02 推送后按 TOC 顺序启动 M-03 Tool Schema 与调用协议。
