---
date: 2026-08-22
topic: M-07 Sandbox 与权限初稿
status: 推送前检查完成
---

# M-07 Sandbox 与权限初稿

## 目标

按串行流水线完成 M-07 的中文 Draft 和 Polish，保留批量 Implementation Review 清单；通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/02-harness-mechanics/sandbox.md`、目录状态、本会话记录和总进度表。
- 范围外：Prompt Injection 防御专题、审批 UI、具体容器编排和批量事实审查。

## 记录与证据

- 已确认 M-06 已由 `dd7e074` 推送，`main` 与远端同步且工作区干净。
- 本章承接审批后的执行边界，处理文件、网络、进程、凭据和资源隔离；不重复审批决策模型。
- 框架对照沿用三个快照版本；具体沙箱实现保留待审。

## 成功标准

- 章节包含理想模型图、双读者解释、权限维度、隔离层级、默认策略和常见坑。
- 区分身份授权、能力约束、运行时隔离和审计。
- Polish 通过后语言清晰；框架行为保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 M-07 是 M-06 后的下一个章节。
- 2026-08-22：主 Agent 完成 M-07 Draft，新增 Sandbox 与权限章节。
- 2026-08-22：主 Agent 直接完成 Polish，统一最小权限、路径边界、凭据代理、运行时隔离和失败关闭术语，强调提示词不能替代强制边界。

## 自检证据

- `node scripts/check-links.mjs` 通过。
- `cd site && npm run check:links` 通过。
- 已检查 diff 只包含 M-07 章节、TOC、本会话记录和总进度表。

## 下一步

M-07 推送后按 TOC 顺序启动 M-08 Retry 与幂等。
