---
date: 2026-08-22
topic: X-03 工具协议对比初稿
status: 推送前检查进行中
---

# X-03 工具协议对比初稿

## 目标

基于第三章工具章节和第二章机制教材，完成 X-03 的中文 Draft 和 Polish；保留批量 Implementation Review 清单，通过链接检查、最小提交和推送后同步目录、会话记录和总进度表。

## 范围

- 范围内：`tutorial/zh-CN/04-comparisons/tools.md`、目录状态、本会话记录和总进度表。
- 范围外：安全审批的系统设计展开、持久化恢复和实验验证。

## 记录与证据

- 已确认 X-02 已由 `b83919b` 推送，`main` 与 `origin/main` 同步且工作区干净。
- 对比输入覆盖 Reasonix 工具契约与执行链、DeepSeek Harness Tools Service 管线、Pi AgentTool 与 Extension Tool Definition。
- 三家共同点是把模型声明、参数校验、调度、治理和结果投影分开；差异在于并发分类器、审批默认值和结果规范化位置。

## 成功标准

- 章节包含统一调用生命周期图和三家协议对照表。
- 明确比较 Schema 声明、名称解析、并发调度、取消配对、大输出处理和扩展点。
- Polish 通过后语言清晰；字段级行为和平台沙箱细节保留在 `pending_review`。
- 两套链接检查通过。

## 进展

- 2026-08-22：建立会话检查点，确认 X-03 是 X-02 后的下一个横向对比章节。
- 2026-08-22：主 Agent 完成 X-03 Draft，覆盖定义面、名称解析、校验治理、并发取消和大输出投影。
- 2026-08-22：主 Agent 直接完成 Polish，统一定义面、执行面、失败关闭、有序提交和模型可见投影术语。

## 自检证据

- 已核对 diff 只包含 X-03 章节、TOC、本会话记录和总进度表。

## 下一步

完成两套链接检查后提交推送；X-04 按 TOC 继续。
