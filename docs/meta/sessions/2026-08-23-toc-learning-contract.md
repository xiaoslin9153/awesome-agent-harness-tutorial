---
date: 2026-08-23
topic: TOC 学习契约与全书递进主线
status: 已完成
---

# TOC 学习契约与全书递进主线

## 目标

执行 `B-004` 的第二步：把目录从「依赖 ID + 状态表」升级为学习契约表，让每章明确继承问题、解决矛盾、核心不变量和遗留问题。

## 范围

- 范围内：`tutorial/zh-CN/TOC.md`、B-004 执行状态、总进度表和本会话记录。
- 范围外：重写任何公开章节；第三章及后续章节的契约扩展留到对应章节重构时处理。

## 决策

1. 在 TOC 前部新增四项学习契约使用规则。
2. 新增全书递进主线 Mermaid 图，说明 C-01 到 M-16 为什么按当前顺序学习。
3. 第一章和第二章表格扩展为 8 列，分别记录继承问题、解决矛盾 / 核心不变量和遗留问题。
4. 第三章及之后暂不扩展列，避免本次改动变成全目录大迁移；这些章节仍保留现有依赖和状态信息。
5. C-01 和 C-02 状态暂标 `已完成`，但其正文是否满足新门禁将在首个深度样板确认后统一复审。

考虑过的替代方案是一次性给全部章节加契约。该方案会让本次提交跨入框架、对比、实验和面试内容，违反最小提交纪律，因此不采用。

## 变更文件

- 更新 `tutorial/zh-CN/TOC.md`
- 更新 `docs/product/backlog/2026-08-23-tutorial-depth-and-progression.md`
- 更新 `docs/product/progress-tracker.md`
- 新增本会话记录

## 验证

1. `cd site && npm run check:links`：45 个 Markdown 文件通过。
2. `cd site && npm run build`：44 页构建成功。
3. `git diff --check` 通过。

## 自检

- 表格分隔行已改为 8 列，避免 Markdown 渲染错位。
- 未修改 CS-02 或其他公开章节正文。
- 未提交构建产物或未跟踪实验文件。
- 契约描述保持为写作要求，不冒充任何框架事实。

## 开放问题

1. C-01 / C-02 正文尚未按新门禁复审，现有完成状态只能视为旧标准下的完成。
2. 第三章框架页需要先确定主题簇粒度，再补契约。
3. 后续章节的继承问题可能随样板重写微调。

## 下一步

1. 提交并推送 TOC 学习契约。
2. 执行部署检查。
3. 重写 `tutorial/zh-CN/01-core-concepts/agent-vs-harness.md` 作为新门禁的首个深度样板。
