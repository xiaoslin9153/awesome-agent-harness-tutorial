# 会话：Q-01 概念与架构题库 v0.3 新建

## 状态与目标

- 状态：进行中，待提交和部署验证。
- 日期：2026-08-23。
- 目标：新建 `tutorial/zh-CN/07-interview/concepts.md`，把第一、二章已验证结论转成带参考答案链的面试题库，完成 Polish 与 Implementation 自检，同步记录并完成部署检查。
- 范围内：只新增 Q-01 公开章节及其 TOC、Backlog、进度表和会话记录。
- 范围外：不修改既有机制章节，不引入新源码事实，不推进 Q-02，不开始批量终审。

## 已完成内容

1. 输入审计：Q-01 原为「未开始」；依赖的 C-01 到 C-04 与 M-01/M-02 均通过 v0.3 Implementation 自检。
2. 证据核对：重新核对 6 个被引用源码区间的行数，全部存在且匹配：
   - DeepSeek Harness `session/src/types.ts:155-173,230-436`
   - Pi `session-manager.ts:1020-1067`
   - Reasonix `sampling_request.go:88-155,157-174`
   - DeepSeek Harness `tools/src/index.ts:1678-1729`
3. 新建题库：10 道核心题，每题包含考察点、「直觉 → 精确机制 → 失效边界」参考答案、常见错误和追问。
4. 内容结构：2 张 Mermaid 图（面试路线图和错误分流图）；5 条故障模式；一条「用户纠正被压缩丢弃」的完整因果链；核心不变量、设计取舍和证据分层表。
5. Front Matter 记录主 Agent Polish 与 Implementation Review 通过，并声明本章只复述依赖章节已验证结论。
6. 同步 TOC 中 Q-01 链接和状态、B-004 执行顺序和进度表 L01 行。

## 决策与证据

1. 采用证据分层：题目引用 C/M/X 章节，章节再引用固定快照锚点；题库不维护第二份行号事实表。
2. 每个错误答案都映射到一条不变量，避免「答案正确但不会迁移」的面试假阳性。
3. 增加错误分流图，让面试官按弱点路由到 CS/L/M 对应章节，而不是直接给答案。
4. 固定快照仍为 Reasonix `aa82b2f`、DeepSeek Harness `b150a55`、Pi `c49906e`。

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 源码锚点 | 6 个引用区间逐个核对，expected/actual 行数一致。 |
| 内容结构 | 2 张 Mermaid 图；5 个故障模式；1 条完整因果链；10 道题均有考察点和三层答案。 |
| 继承证据 | 引用 C-01/C-04、M-01/M-02、X-04、L-02/L-03、CS-01/CS-02 等已自检页面。 |
| 链接检查 | `cd site && npm run check:links` 通过，检查 47 个文件。 |
| 本地构建 | `cd site && npm run build` 通过，构建 46 个页面。 |
| Git 检查 | `git diff --check` 通过；当前变更限定在 Q-01 及必要状态同步。 |
| 提交部署 | 待执行。 |

## 开放问题

1. 是否需要为产品术语与本教程术语建立映射表；当前保留在自检问题中。
2. 批量终审时若 M-06/X-04 的审批终态措辞调整，Q10 必须同步。
3. 电话面主轴选题尚未和维护者确认。

## 下一步

1. 只提交本章及状态同步文件，推送后检查 GitHub Actions 和线上页面。
2. 回填部署证据并标记会话完成。
3. 下一次迭代处理 Q-02《实现与调试题》。
